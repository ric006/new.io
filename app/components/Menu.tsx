// components/Menu.tsx

import React from 'react';

const Menu: React.FC = () => {
  return (
    <header>
      {/* Add header content, including Connect button */}
    </header>
  );
};

export default Menu;
